{
  "name": "jdbc-sink-expenses-per-item",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "results.expenses-per-item",
    "connection.url": "jdbc:postgresql://database:5432/project3",
    "connection.user": "postgres",
    "connection.password": "nopass",
    "table.name.format": "res_expenses_per_item",
    "insert.mode": "upsert",
    "pk.mode": "record_key",
    "pk.fields": "pet_type_id",
    "auto.create": "false",
    "auto.evolve": "false",
    "key.converter": "org.apache.kafka.connect.converters.LongConverter",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "true"
  }
}
