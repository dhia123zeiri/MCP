package com.doctor_office.config;

import org.springframework.context.annotation.Configuration;

/**
 * Kafka Streams logic now lives in the standalone `streams-app` module
 * (per the assignment's "stand-alone applications" requirement).
 *
 * This class is intentionally empty — kept so any old wiring still
 * compiles. Do NOT start a Streams instance here.
 */
@Configuration
public class KafkaStreamsConfig {
}
